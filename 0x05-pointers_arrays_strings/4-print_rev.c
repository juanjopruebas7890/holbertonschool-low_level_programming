#include "holberton.h"
/**
 * print_rev - is the prototype
 *@s: is the variable we are working with
 *
 */
void print_rev(char *s)
{
	int r;


	for (r = 0; s[x] != '\0' ; r++)
	{
	}

	for (r = r - 1; r >= 0; r--)
	{
		_putchar(s[r]);
	}
	_putchar('\n');
}
