#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	int l=10;
	int z;
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	z = n % l
		if (n > 5)

pritnf("Last digit of %i is %i and is greater than 5 \n", n, l);


	
}
